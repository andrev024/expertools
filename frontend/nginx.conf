server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Intenta servir el archivo pedido; si no existe, sirve index.html
        # y deja que React Router decida que componente mostrar.
        try_files $uri $uri/ /index.html;
    }
}
